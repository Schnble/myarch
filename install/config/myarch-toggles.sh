# Toggles are used to turn certain features on/off in a persistent way
mkdir -p ~/.local/state/myarch/toggles/hypr
cp "$MYARCH_PATH/default/hypr/toggles/flags.lua" ~/.local/state/myarch/toggles/hypr/

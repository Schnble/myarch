#!/bin/bash

# myarch:summary=Launch a TUI command in the default terminal with Myarch styling
# myarch:args=<command> [args...]

exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.myarch.$(basename $1) -e "$1" "${@:2}"

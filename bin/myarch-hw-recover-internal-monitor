#!/bin/bash

# myarch:summary=Clear the internal-monitor-disable toggle if no external display is connected.

TOGGLE="$HOME/.local/state/myarch/toggles/hypr/internal-monitor-disable.lua"

if [[ -f $TOGGLE ]] && ! myarch-hw-external-monitors; then
  rm -f "$TOGGLE"
fi

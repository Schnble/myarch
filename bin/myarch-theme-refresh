#!/bin/bash

# myarch:summary=Refresh the current theme from its templates.

THEME_NAME_PATH="$HOME/.config/myarch/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  MYARCH_THEME_SKIP_BACKGROUND=1 myarch-theme-set "$(cat $THEME_NAME_PATH)"
fi

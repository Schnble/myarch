#!/bin/bash

# myarch:summary=Wrapper for swayosd-client that targets the currently focused monitor.
# myarch:args=<swayosd-client-args...>

exec swayosd-client --monitor "$(myarch-hyprland-monitor-focused)" "$@"
